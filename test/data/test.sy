// 这个样例是性能测试第一个样例, 不知道为什么原本的程序也跑不通, 时间函数用下面的简单测试替换
// int lim;

// int fun(int n,int dep){
// 	if(n==1) return dep;
// 	else{
// 		if(n%2==0) return fun(n/2,dep+1);
// 		else{
// 			if(n*3+1<=lim)
// 				return fun(n*3+1,dep+1);
// 			else
// 				return 0;
// 		}
// 	}
// }

// const int mod = 1000000007;

// int main(){
// 	lim = getint();
// 	int ans = 0;
// 	int i = 1;
//         starttime();
// 	while(i<=lim){
// 		ans = (ans+fun(i,0))%mod;
// 		i = i+1;
// 	}
//         stoptime();
// 	putint(ans);
// 	putch(10);
// }



int main() {
  starttime();
  int a[10][10] = {{3}, {1, 4}, {}, 1, 5, 9, 2, 6, 5, 3, 5, 8, 9, {}, {7}};
  int i = 0, j = 0;
  while (i < 10) {
    j = 0;
    while (j < 10) {
      putint(a[i][j]);
      putch(32);
      j = j + 1;
    }
    putch(10);
    i = i + 1;
  }
  int k = 0;
  while (k < 10000) {
	k = k + 1;
  }
  stoptime();
  return 0;
}