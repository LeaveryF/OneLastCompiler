float p = 5;
const float cp = 2;
float arr[5] = {1, 2, 3, 4, 5};
const float ks[2] = {1.2, 3.4};

int main(){
  float x = 1.2;
  putfloat(x + p + cp);
  putint(x + p);

  putfarray(5, arr);

  return x + p + ks[1];
}